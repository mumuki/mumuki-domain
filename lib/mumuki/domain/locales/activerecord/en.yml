---
en:
  activerecord:
    errors:
      models:
        guide:
          attributes:
            base:
              in_use: 'Guide is still in use in organization %{organization}'
        topic:
            attributes:
              base:
                in_use: 'Topic is still in use in organization %{organization}'
        exercise:
          attributes:
            randomizations:
              invalid_format: 'format is invalid'
            own_expectations:
              invalid_format: 'format is invalid'
            assistance_rules:
              invalid_format: 'format is invalid'
            name:
              invalid_format: 'must not contain /'
            base:
              evaluation_criteria_required:  'You need to provide tests and/or expectations'
        organization:
          attributes:
            base:
              consistent_public_login: 'A public organization can not restrict login methods'
